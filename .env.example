# Database
DB_HOST=localhost
DB_PORT=5432
DB_NAME=campaign_manager
DB_USER=postgres
DB_PASSWORD=postgres
DB_NAME_TEST=campaign_manager_test

# JWT
JWT_SECRET=your-super-secret-key-minimum-32-characters-long-here

# Server
PORT=3000
NODE_ENV=development

# SMTP (Mailpit default for local dev)
SMTP_HOST=localhost
SMTP_PORT=1025
SMTP_SECURE=false
SMTP_USER=
SMTP_PASS=
SMTP_FROM=campaign-manager@localhost

# Frontend
VITE_API_URL=http://localhost:3000
